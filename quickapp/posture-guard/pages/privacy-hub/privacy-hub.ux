<template>
  <div class="page">
    <!-- 顶部导航 -->
    <div class="header">
      <div class="back-btn" onclick="goBack">
        <text class="back-text">←</text>
      </div>
      <text class="title">🛡️ 隐私中心</text>
      <div class="placeholder"></div>
    </div>

    <!-- 隐私状态卡片 -->
    <div class="status-card">
      <div class="status-row">
        <text class="status-icon">🔒</text>
        <div class="status-info">
          <text class="status-title">数据保护状态</text>
          <text class="status-value active">已激活</text>
        </div>
      </div>
      <div class="status-details">
        <div class="detail-item">
          <text class="detail-label">加密级别</text>
          <text class="detail-value">AES-256-GCM</text>
        </div>
        <div class="detail-item">
          <text class="detail-label">脱敏引擎</text>
          <text class="detail-value">差分隐私 ε=0.1</text>
        </div>
        <div class="detail-item">
          <text class="detail-label">密钥分片</text>
          <text class="detail-value">Shamir 2/3</text>
        </div>
      </div>
    </div>

    <!-- 数据存储统计 -->
    <div class="storage-card">
      <text class="card-title">存储统计</text>
      <div class="storage-grid">
        <div class="storage-item">
          <text class="storage-value">{{ hotCount }}</text>
          <text class="storage-label">热数据</text>
          <text class="storage-age">24h内</text>
        </div>
        <div class="storage-item">
          <text class="storage-value">{{ warmCount }}</text>
          <text class="storage-label">温数据</text>
          <text class="storage-age">1-7天</text>
        </div>
        <div class="storage-item">
          <text class="storage-value">{{ coldCount }}</text>
          <text class="storage-label">冷数据</text>
          <text class="storage-age">7-30天</text>
        </div>
        <div class="storage-item">
          <text class="storage-value">{{ auditCount }}</text>
          <text class="storage-label">审计日志</text>
          <text class="storage-age">加密存储</text>
        </div>
      </div>
    </div>

    <!-- 审计日志预览 -->
    <div class="audit-card">
      <text class="card-title">最近审计日志</text>
      <div class="audit-list">
        <div class="audit-item" for="{{ recentLogs }}">
          <div class="audit-time">{{ $item.time }}</div>
          <div class="audit-event">{{ $item.event }}</div>
          <div class="audit-result {{ $item.resultClass }}">{{ $item.result }}</div>
        </div>
      </div>
      <div class="audit-more" onclick="viewAllLogs">
        <text class="audit-more-text">查看全部日志 →</text>
      </div>
    </div>

    <!-- 快捷操作 -->
    <div class="actions-card">
      <text class="card-title">快捷操作</text>
      <div class="action-buttons">
        <div class="action-btn" onclick="exportAuditLogs">
          <text class="action-icon">📋</text>
          <text class="action-text">导出审计日志</text>
        </div>
        <div class="action-btn" onclick="clearExpiredData">
          <text class="action-icon">🗑️</text>
          <text class="action-text">清理过期数据</text>
        </div>
        <div class="action-btn" onclick="viewKeyStatus">
          <text class="action-icon">🔑</text>
          <text class="action-text">密钥状态</text>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
/**
 * 隐私中心页面
 *
 * 展示隐私保护状态、存储统计、审计日志等信息。
 * 下阶段将填充完整 UI 和交互功能。
 *
 * @module pages/privacy-hub
 */

import router from '@system.router'
import prompt from '@system.prompt'

export default {
  private: {
    // 存储统计
    hotCount: 12,
    warmCount: 45,
    coldCount: 23,
    auditCount: 7,

    // 审计日志预览
    recentLogs: [
      { time: '14:32', event: '数据写入', result: '成功', resultClass: 'success' },
      { time: '14:30', event: '传感器脱敏', result: '完成', resultClass: 'success' },
      { time: '14:28', event: '加密存储', result: 'L2级别', resultClass: 'info' },
      { time: '14:25', event: '数据销毁', result: 'L3过期', resultClass: 'warning' },
    ],
  },

  onReady() {
    console.log('[PrivacyHub] onReady')
    this._loadStats()
  },

  /**
   * 加载统计数据
   * @private
   */
  _loadStats() {
    // TODO: 从 LifecycleManager 和 AuditLogger 获取真实数据
    console.log('[PrivacyHub] Loading stats...')
  },

  /**
   * 返回上一页
   */
  goBack() {
    router.back()
  },

  /**
   * 查看全部日志
   */
  viewAllLogs() {
    prompt.showToast({ message: '日志查看功能开发中...' })
  },

  /**
   * 导出审计日志
   */
  exportAuditLogs() {
    prompt.showToast({ message: '日志导出功能开发中...' })
  },

  /**
   * 清理过期数据
   */
  clearExpiredData() {
    prompt.showToast({ message: '清理功能开发中...' })
  },

  /**
   * 查看密钥状态
   */
  viewKeyStatus() {
    prompt.showToast({ message: '密钥状态功能开发中...' })
  },
}
</script>

<style>
.page {
  flex-direction: column;
  width: 480px;
  height: 480px;
  background-color: #0a0a0a;
}

/* 顶部导航 */
.header {
  width: 100%;
  height: 50px;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  padding: 0 15px;
  background-color: #1a1a2e;
}

.back-btn {
  width: 30px;
  height: 30px;
  justify-content: center;
  align-items: center;
}

.back-text {
  font-size: 20px;
  color: #ffffff;
}

.title {
  font-size: 18px;
  color: #ffffff;
  font-weight: bold;
}

.placeholder {
  width: 30px;
}

/* 隐私状态卡片 */
.status-card {
  width: 420px;
  padding: 15px;
  background-color: #1a1a2e;
  border-radius: 12px;
  margin: 10px auto;
}

.status-row {
  flex-direction: row;
  align-items: center;
}

.status-icon {
  font-size: 30px;
  margin-right: 12px;
}

.status-info {
  flex-direction: column;
}

.status-title {
  font-size: 14px;
  color: #888888;
}

.status-value {
  font-size: 18px;
  color: #00d4aa;
  font-weight: bold;
}

.status-value.active {
  color: #00d4aa;
}

.status-details {
  margin-top: 12px;
  padding-top: 12px;
  border-top-width: 1px;
  border-top-color: #2a2a4e;
}

.detail-item {
  flex-direction: row;
  justify-content: space-between;
  margin-bottom: 6px;
}

.detail-label {
  font-size: 13px;
  color: #aaaaaa;
}

.detail-value {
  font-size: 13px;
  color: #ffffff;
}

/* 存储统计卡片 */
.storage-card {
  width: 420px;
  padding: 15px;
  background-color: #1a1a2e;
  border-radius: 12px;
  margin: 10px auto;
}

.card-title {
  font-size: 15px;
  color: #888888;
  margin-bottom: 12px;
}

.storage-grid {
  flex-direction: row;
  justify-content: space-between;
}

.storage-item {
  flex-direction: column;
  align-items: center;
  width: 90px;
}

.storage-value {
  font-size: 24px;
  color: #00d4aa;
  font-weight: bold;
}

.storage-label {
  font-size: 12px;
  color: #ffffff;
  margin-top: 4px;
}

.storage-age {
  font-size: 10px;
  color: #666666;
  margin-top: 2px;
}

/* 审计日志卡片 */
.audit-card {
  width: 420px;
  padding: 15px;
  background-color: #1a1a2e;
  border-radius: 12px;
  margin: 10px auto;
}

.audit-list {
  flex-direction: column;
}

.audit-item {
  flex-direction: row;
  align-items: center;
  padding: 8px 0;
  border-bottom-width: 1px;
  border-bottom-color: #2a2a4e;
}

.audit-time {
  font-size: 12px;
  color: #666666;
  width: 60px;
}

.audit-event {
  font-size: 13px;
  color: #ffffff;
  flex: 1;
}

.audit-result {
  font-size: 12px;
  padding: 2px 8px;
  border-radius: 4px;
}

.audit-result.success {
  color: #00d4aa;
  background-color: #1a3a2e;
}

.audit-result.info {
  color: #4a9eff;
  background-color: #1a2a3e;
}

.audit-result.warning {
  color: #ffc107;
  background-color: #3a2a1e;
}

.audit-more {
  margin-top: 10px;
  align-items: center;
}

.audit-more-text {
  font-size: 13px;
  color: #6c5ce7;
}

/* 快捷操作卡片 */
.actions-card {
  width: 420px;
  padding: 15px;
  background-color: #1a1a2e;
  border-radius: 12px;
  margin: 10px auto;
}

.action-buttons {
  flex-direction: row;
  justify-content: space-between;
}

.action-btn {
  flex-direction: column;
  align-items: center;
  width: 120px;
  padding: 12px 0;
  background-color: #2a2a4e;
  border-radius: 8px;
}

.action-icon {
  font-size: 24px;
  margin-bottom: 6px;
}

.action-text {
  font-size: 12px;
  color: #ffffff;
}
</style>
