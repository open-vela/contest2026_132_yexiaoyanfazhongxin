<template>
  <div class="timeline-container">
    <div class="timeline-item" for="{{ items }}">
      <div class="timeline-left">
        <div class="timeline-dot {{ $item.type }}"></div>
        <div class="timeline-line" if="{{ $index < items.length - 1 }}"></div>
      </div>
      <div class="timeline-content">
        <div class="timeline-header">
          <text class="timeline-time">{{ $item.time }}</text>
          <div class="timeline-tag {{ $item.type }}">
            <text class="timeline-tag-text">{{ $item.tag }}</text>
          </div>
        </div>
        <text class="timeline-desc">{{ $item.description }}</text>
      </div>
    </div>
  </div>
</template>

<script>
/**
 * 时间线组件
 *
 * 用于隐私中心审计日志展示，支持：
 * - 垂直时间线布局
 * - 事件类型标签（不同颜色）
 * - 时间戳显示
 *
 * @module common/components/timeline
 */

export default {
  props: {
    items: { type: Array, default: [] },
  },
}
</script>

<style>
.timeline-container {
  flex-direction: column;
  width: 100%;
  padding: 10px 0;
}

.timeline-item {
  flex-direction: row;
  margin-bottom: 16px;
}

.timeline-left {
  width: 30px;
  flex-direction: column;
  align-items: center;
}

.timeline-dot {
  width: 12px;
  height: 12px;
  border-radius: 6px;
  margin-top: 4px;
}

.timeline-dot.access {
  background-color: #2979FF;
}

.timeline-dot.write {
  background-color: #00C853;
}

.timeline-dot.delete {
  background-color: #FF1744;
}

.timeline-dot.encrypt {
  background-color: #9C27B0;
}

.timeline-dot.lifecycle {
  background-color: #FFD600;
}

.timeline-line {
  width: 2px;
  flex: 1;
  background-color: #2a2a4e;
  margin-top: 4px;
}

.timeline-content {
  flex: 1;
  margin-left: 10px;
}

.timeline-header {
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
}

.timeline-time {
  font-size: 12px;
  color: #666666;
}

.timeline-tag {
  padding: 2px 8px;
  border-radius: 4px;
}

.timeline-tag.access {
  background-color: rgba(41, 121, 255, 0.2);
}

.timeline-tag.write {
  background-color: rgba(0, 200, 83, 0.2);
}

.timeline-tag.delete {
  background-color: rgba(255, 23, 68, 0.2);
}

.timeline-tag.encrypt {
  background-color: rgba(156, 39, 176, 0.2);
}

.timeline-tag.lifecycle {
  background-color: rgba(255, 214, 0, 0.2);
}

.timeline-tag-text {
  font-size: 11px;
  color: #ffffff;
}

.timeline-desc {
  font-size: 13px;
  color: #ffffff;
  margin-top: 4px;
}
</style>
